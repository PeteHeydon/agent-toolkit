# Steering — {{AGENT_NAME}}

Guidance you've thought about offline, as opposed to turn-by-turn correction in
chat. Write it here, then ask the agent to read this file and apply it.

Keep it as standing instructions, not a change log: "always name the owner of
an action item, or say the owner is unclear" rather than "last time you forgot
the owners". The agent reads what's here now, not what you were reacting to
when you wrote it.

Guidance that you'd want from *every* agent belongs in the baseline profile
instead — see `~/.agent-toolkit/baseline.yaml`.
